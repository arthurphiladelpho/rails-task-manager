<h1>
  <%= @task.title %>
</h1>
<p>
  <%= @task.details %>
  <%= check_box_tag 'completed', 'is_done', @task.completed %>
</p>
<%= link_to 'Edit', edit_path(@task) %>
<%= link_to 'Delete', task_path(@task), method: :delete,
      data: { confirm: "Are you sure?" } %>

